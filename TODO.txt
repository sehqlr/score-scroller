Write the Javascript in jQuery. As a note, you can use a regular for loop in jQuery.
	Use this to redraw the first canvas onto the second canvas

Once the basic scrolling functionality is finished, the app is at
	version 0.5! This would be a good time to celebrate a little

Obtain ten or so scores to demo with the app. Make sure we get the
	MusicXML files

Change the User Input controls to fit our program. Use jQuery UI

Create the server setup to host the app

Pretty it up with web design

Bugfixes

Ship it as version 1.0!

NOTES:

Ignore error checking
https://developers.google.com/closure/
